# BDCinema Next.js Application Environment Variables

# TMDB API Key for populating movie and series data
# Get one at https://developer.themoviedb.org/docs/getting-started
TMDB_API_KEY="your_tmdb_api_key_here"

# NextAuth Secret for encrypting JWT session cookies
# Generate one using `openssl rand -base64 32` or `npx auth secret`
NEXTAUTH_SECRET="your_generated_secret_here"

# NextAuth URL
# Must be set to your deployed domain in production (e.g., https://bdcinema.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
